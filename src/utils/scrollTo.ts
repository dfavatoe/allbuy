// const registerRef = useRef<any | null>(null);

// const scrollCallback = () => {
//   registerRef.current.scrollIntoView({ behavior: "smooth" });
// };

// onClick={() => scrollCallback()}

//<span ref={registerRef}></span>
